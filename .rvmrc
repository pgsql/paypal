if [[ -n "$rvm_environments_path" && -s "$rvm_environments_path/ruby-1.9.2@calculator" ]] ; then
  . "$rvm_environments_path/ruby-1.9.2@calculator"
else
  rvm --create use  "ruby-1.9.2@calculator"
fi
